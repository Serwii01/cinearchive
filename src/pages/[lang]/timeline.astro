---
// La cronología vive ahora dentro de /archivo (sección plegable).
import { localizePath, type Lang } from '../../i18n/ui';
const lang = (Astro.params.lang === 'en' ? 'en' : 'es') as Lang;
return Astro.redirect(localizePath(lang, 'archive') + '#cronologia');
---
