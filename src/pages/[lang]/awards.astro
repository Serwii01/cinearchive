---
// El palmarés vive ahora dentro de /archivo (sección plegable).
import { normalizeLang, localizePath, type Lang } from '../../i18n/ui';
const lang = normalizeLang(Astro.params.lang);
return Astro.redirect(localizePath(lang, 'archive') + '#palmares');
---
