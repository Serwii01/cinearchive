---
// Las listas personalizadas se gestionan dentro de "Mi lista" (watchlist).
import { normalizeLang, localizePath, type Lang } from '../../../i18n/ui';
const lang = normalizeLang(Astro.params.lang);
return Astro.redirect(localizePath(lang, 'watchlist'));
---
